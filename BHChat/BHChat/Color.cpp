#include "Color.h"

// Constructors
Color::Color() : r(0), g(0), b(0)															{}
Color::Color(int newR, int newG, int newB) : r(newR), g(newG), b(newB)						{}

// Destructors
Color::~Color()																				{}
